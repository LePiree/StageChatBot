# Aucune clé API requise — le LLM tourne en local via Ollama
# Le modèle par défaut est llama3.2:1b (~1.3 GB, téléchargé automatiquement au premier lancement)
# Pour changer de modèle, modifier OLLAMA_MODEL dans docker-compose.yml